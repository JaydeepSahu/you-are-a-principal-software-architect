apiVersion: v1
kind: Service
metadata:
  name: aigateway-service
  namespace: enterprise-ai-platform
  labels:
    app.kubernetes.io/name: aigateway-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: aigateway-api
